<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Premis</title>

    <link rel="stylesheet" href="/css/paginaPrincipal.css">

    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
</head>

<body>

    <header class="page-header">
        <div class="header-left">
            <a href="/" class="back-link">
                <i class="fas fa-arrow-left"></i>
            </a>
            <h1 class="page-title">Llistat de Premis</h1>
        </div>

<<<<<<< HEAD
        <div class="header-right">
            <a href="/prizes/form" class="add-button">
                <i class="fas fa-plus"></i> Afegir nou premi
            </a>
        </div>
    </header>
    <div class="flex-container">

        <main class="main-content">

            <!-- BARRA DE BÚSQUEDA -->
            <div class="search-bar">
                <i class="fas fa-search"></i>
                <input type="text" placeholder="Buscar per nom, any, tipus..." onkeyup="filterPrizes(this.value)">
            </div>

            <% if (prizes && prizes.length> 0) { %>

                <div class="architect-list">

                    <% prizes.forEach(prize=> { %>

                        <a href="/prizes/edit/<%= prize.id_prize %>" class="architect-card-link">
                            <div class="architect-card" data-name="<%= prize.name %>" data-year="<%= prize.year %>"
                                data-tipe="<%= prize.tipe %>">

                                <div class="card-header">

                                    <div class="card-title-group">

                                        <h3>
                                            <%= prize.name %>
                                        </h3>

                                        <div class="card-meta-info">
                                            <span class="dates">(<%= prize.year %>)</span>
                                            <span class="nationality">Tipus: <%= prize.tipe %></span>
                                        </div>
                                    </div>

                                    <button type="button" onclick="deletePrize('<%= prize.id_prize %>')"
                                        class="delete-button">
                                        <i class="fas fa-trash-alt"></i>
                                    </button>

                                </div>

                            </div>
                        </a>

                        <% }); %>

                </div>

                <% } else { %>

                    <p class="no-architects">No hi ha premis registrats.</p>

                    <% } %>

        </main>
    </div>
    <script src="/scripts/prizes/prizes.js"></script>
=======
    <div class="header-right">
      <a href="/prizes/form" class="add-button">
        <i class="fas fa-plus"></i> Afegir nou premi
      </a>
    </div>
  </header>
  <div class="flex-container">

    <main class="main-content">

      <!-- BARRA DE BÚSQUEDA -->
      <div class="search-bar">
        <i class="fas fa-search"></i>
        <input type="text" placeholder="Buscar per nom, any, tipus..." onkeyup="filterPrizes(this.value)">
      </div>
      <div class="architect-list">
        <% if (prizes && prizes.length> 0) { %>
          <% prizes.forEach(prize=> { %>

            <div class="architect-card-container">
              <div class="holis">
                <a href="/prizes/edit/<%= prize.id_prize %>" class="architect-card-link">
                  <div class="architect-card" data-name="<%= prize.name %>" data-year="<%= prize.year %>"
                    data-tipe="<%= prize.tipe %>">

                    <div class="card-header">

                      <div class="card-title-group">

                        <h3>
                          <%= prize.name %>
                        </h3>

                        <div class="card-meta-info">
                          <span class="dates">(<%= prize.year %>)</span>
                          <span class="nationality">Tipus: <%= prize.tipe %></span>
                        </div>
                      </div>
                    </div>
                  </div>
                </a>

                <button type="button" class="delete-button" onclick="deletePrize('<%= prize.id_prize %>')">
                  <img src="/images/icons/trash-2-64.png" alt="Borrar">
                </button>
              </div>

              <% }); %>
                <% } else { %>

                  <p class="no-architects">No hi ha premis registrats.</p>

                  <% } %>
            </div>
      </div>
    </main>
  </div>
  <script src="/scripts/prizes/prizes.js"></script>
>>>>>>> e03b02b (Refactor card layouts and improve delete button design across multiple views)
</body>

</html>