<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Publicacions</title>
    <link rel="stylesheet" href="/css/paginaPrincipal.css"> 
</head>
<body>
    <div class="page-header">
        <div class="header-left">
            <a href="/" class="back-link" aria-label="Tornar al menú principal">
                <!-- <i class="fas fa-arrow-left"></i> --> &#8592; Tornar al menú principal
            </a>
            <h1 class="page-title">Llistat de publicacions</h1>
        </div>
        <div class="header-right">
            <a href="/publications/form" class="add-button">
                <!-- <i class="fas fa-plus"></i> --> Afegir nova publicació
            </a>
        </div>
    </div>

    <div class="main-content">
        <% if (publications && publications.length > 0) { %>
            <div class="architect-list"> <!-- Usamos architect-list para envolver las tarjetas -->
                <% publications.forEach(publication => { %>
                    <div class="architect-card"> <!-- Cada publicación es una tarjeta -->
                        <div class="card-header">
                            <div class="card-title-group">
                                <a href="/publications/edit/<%= publication.id_publication %>" class="edit-icon" aria-label="Editar publicació">
                                    <!-- <i class="fas fa-edit"></i> --> &#9998;
                                </a>
                                <h3><%= publication.title %></h3>
                            </div>
                            <button type="button" class="delete-button" onclick="deletePublication('<%= publication.id_publication %>')" aria-label="Eliminar publicació">
                                <!-- <i class="fas fa-trash-alt"></i> --> &#128465;
                            </button>
                        </div>
                        <p class="card-description"><%= publication.description %></p>
                        <!-- Si tuvieras más información de la publicación (fecha, autor, etc.)
                             podrías usar card-footer o card-meta-info aquí. Ejemplo:
                        <div class="card-footer">
                            <i class="fas fa-calendar-alt"></i> Fecha de publicación: 01/01/2023
                        </div>
                        -->
                    </div>
                <% }); %>
            </div>
        <% } else { %>
            <p class="no-architects">No hi ha publicacions disponibles.</p>
        <% } %>
    </div>
    <script src="/scripts/publications/publications.js"></script>
</body>
</html>