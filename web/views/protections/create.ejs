<!DOCTYPE html>
<html lang="ca">

<head>
    <%- include('../partials/head', { title: 'Nova Protecció' , cssType: 'form' }) %>
</head>

<body>
    <!-- Encabezado del formulario -->
    <%- include('../partials/headerForm', { backUrl: '/protections' , title: 'Nova Protecció' }) %>

        <!-- Contenedor principal del formulario -->
        <main class="flex-container">
            <section class="form-container">
                <!-- Formulario para añadir nueva protección -->
                <form id="form-protection" class="api-form" action="/protections/create" method="POST">
                    <!-- Campos del formulario -->
                    <div class="form-group">
                        <label for="level">Nivell <span class="required">*</span></label>
                        <input type="text" id="level" name="level" required />
                    </div>

                    <div class="form-group">
                        <label for="description">Descripció</label>
                        <textarea id="description" name="description"></textarea>
                    </div>

                    <!-- Botón de envío -->
                    <button type="submit" class="submit-button">
                        <i class="fas fa-save"></i> Desar
                </form>
            </section>
        </main>


</body>

</html>