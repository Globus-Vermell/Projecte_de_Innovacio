<!DOCTYPE html>
<html lang="es">

<head>
    <%- include('../partials/head', { title: 'Editar Protecció' , cssType: 'form' }) %>
</head>

<body>

    <!-- Encabezado de la página -->
    <%- include('../partials/headerForm', { backUrl: '/protections' , title: 'Editar Protecció' }) %>

        <main class="flex-container">

            <!-- Contenedor del formulario -->
            <section class="form-container">
                <form id="main-form" class="api-form"
                    action="/protections/edit/<%= protection.id_protection %>" method="PUT"
                    data-redirect="/protections">

                    <div class="form-group">
                        <label for="level">Nivel <span class="required">*</span></label>
                        <input type="text" id="level" name="level" value="<%= protection.level %>" required />
                    </div>

                    <div class="form-group">
                        <label for="description">Descripción</label>
                        <textarea id="description" name="description"><%= protection.description %></textarea>
                    </div>
                </form>
            </section>

        </main>

</body>

</html>