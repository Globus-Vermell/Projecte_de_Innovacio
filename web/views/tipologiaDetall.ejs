<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= typology.name %></title>
</head>
<body>
  <h1><%= typology.name %></h1>

  <% if (typology.image) { %>
    <img src="<%= typology.image %>" alt="<%= typology.name %>" width="300">
  <% } %>

  <a href="/tipologia">Volver al listado</a>
</body>
</html>