<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Premios</title>
</head>
<body>
  <h1>Listado de Premios</h1>

  <% if (prizes && prizes.length > 0) { %>
    <ul>
      <% prizes.forEach(prize => { %>
        <li>
          <strong><%= prize.name %></strong> 
          (<%= prize.year %>) — Tipo: <%= prize.tipe %>  
          — <a href="/premios/<%= prize.id_prize %>">Ver detalle</a>
        </li>
      <% }); %>
    </ul>
  <% } else { %>
    <p>No hay premios registrados.</p>
  <% } %>
</body>
</html>