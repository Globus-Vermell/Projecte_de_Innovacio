<!DOCTYPE html>
<html lang="ca">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Reforma</title>
    <link rel="stylesheet" href="/css/editForm.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">

<body>
    <header class="form-header">
        <a href="/reform" class="back-link"><i class="fas fa-arrow-left"></i></a>
        <h1 class="page-title">Editar Reforma</h1>
    </header>
    <div class="flex-container">
        <main class="form-container">
            <form id="form-reform-edit">
                <div class="form-group">
                    <label for="year">Any de la reforma <span class="required">*</span></label>
                    <input type="number" id="year" name="year" value="<%= reform.year %>" required />
                </div>

                <div class="form-group">
                    <label for="id_architect">Arquitecte <span class="required">*</span></label>
                    <select id="id_architect" name="id_architect" required>
                        <option value="">-- Selecciona un arquitecte --</option>
                        <% architects.forEach(arq=> { %>
                            <option value="<%= arq.id_architect %>" <%=arq.id_architect===reform.id_architect
                                ? 'selected' : '' %>><%= arq.name %>
                            </option>
                            <% }) %>
                    </select>
                </div>

                <button type="submit" class="submit-button"><i class="fas fa-save"></i> Guardar canvis</button>
            </form>
        </main>
    </div>
    <script>
        const reform = JSON.parse('<%- JSON.stringify(reform) %>');
    </script>
    <script src="/scripts/reform/reformEdit.js"></script>
</body>

</html>